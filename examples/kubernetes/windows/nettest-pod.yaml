apiVersion: v1
kind: Pod
metadata:
  name: nettest
spec:
  nodeName: e2e-test-peterhornyack-windows-node-group-1cnn
  containers:
   - name: nettest
     image: e2eteam/nettest:1.0
     volumeMounts:
       - mountPath: /share
         name: pvfs
  volumes:
   - name: pvfs
     persistentVolumeClaim:
       claimName: pvc-filestore
